export enum ScreenNames {
  // the root that wraps the tabs
  Root = "Root",
  // home tab and page
  Home = "Home",
  // the inbox tab and page
  Inbox = "Inbox",
  // an individual message
  Message = "Message",
  // modal that appears for filtering on home
  HomeSettingsModal = "Home settings modal",
  // the profile of the user
  Profile = "Profile",
  // the photographer profile
  PhotographerProfile = " Photographer profile",
}
